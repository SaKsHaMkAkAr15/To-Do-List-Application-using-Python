# To-Do-List-Application-using-Python
This is  command-line To Do List application which is built in Python which allows users to add tasks, view them, mark them as done, and delete them. In this all tasks are saved persistently using CSV files so your list remains intact even after closing the program.
